import React, {PropTypes} from 'react';

const Calendar = ({
  user,
}) => (
  <div>
   <p>Hello</p>
  </div>
);

Calendar.propTypes = {
   user: PropTypes.object.isRequired
};

export default Calendar;
